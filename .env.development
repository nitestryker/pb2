# Development environment variables
# By default, use production backend (no local setup required)
VITE_API_BASE_URL=https://pb2-ahh9.onrender.com/api

# Uncomment to use local backend (requires local server running on port 3001)
# VITE_USE_LOCAL_BACKEND=true
# VITE_API_BASE_URL=http://localhost:3001/api

# Fallback configuration
VITE_ENABLE_API_FALLBACK=true
VITE_API_TIMEOUT=30000

